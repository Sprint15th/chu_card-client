import { FormStateContext } from '@/context/FormStateContext';
import React, { useContext } from 'react'

export const CakePreview = () => {

  // const  { form } = useContext(FormStateContext);

 
  // 사용자가 케이크 토핑, 외형을 선택했을 때 상태들이 변경
  return (
    <div>CakePreview</div>
  )
};
